# TODO (Swagger installation)

- [ ] Install `@nestjs/swagger` and `swagger-ui-express` for `apps/backend` using pnpm.
- [ ] Update `apps/backend/src/main.ts` to mount Swagger UI at `/api/docs`.
- [ ] Run `pnpm --filter backend build` (or equivalent) to ensure TypeScript compiles.
- [ ] Run backend and verify Swagger endpoint loads.

